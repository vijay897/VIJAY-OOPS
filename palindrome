#include<iostream>
using namespace std;
int main(){
    int num,rem,res=0,number;
    
    cout<<"enter the number";
    cin>>num;
    number = num;
    while(num != 0){
        rem = num%10;
        res = res*10+rem;
        num = num/10;
    }
    if(res==number)
    cout<<"ok";
    else
    cout<<"not";
}
