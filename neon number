#include <iostream>
using namespace std;
int main()
{
  int n,sum=0;
  cout<<"Enter a Number:";
  cin>>n;
  int s=n*n;
  while(s>0)
  {
    sum+=s%10;
    s/=10;
  }
  if(sum==n)
  {
    cout<<n<<" is a neon Number"<<endl;
  }
  else
    cout<<n<<" is not a Neon Number"<<endl;
  return 0;
}
